# Generate Why Us / Regulated

Use only the uploaded sources.

## Hard rules

- Sell `genu.mark` first.
- Keep `genu.mark` as the core.
- Make it obvious that the buyer is choosing the safer implementation partner, not just comparing equipment or abstract software.
- Show the ERP split explicitly.
- Keep `еАкциз` external.
- Do not drift into generic company bragging, ecosystem talk, or platform-first language.
- Do not open with `genu.im`.
- Do not introduce any new product, architecture layer, platform explanation, or selling object not present in the uploaded sources.
- Do not use weak claims such as `best equipment`, `turnkey`, `single responsibility`, or `full package` without explicit proof.
- Do not use unsafe claims such as `гарантує`, `найкраще`, `під ключ`, `єдина відповідальність`, or `повний пакет`.
- Keep the output in modern, buyer-facing Ukrainian.
- End with a clear next step to scoped contour review / audit / pilot.

## Output contract

Return: `Headline`, `Subheadline`, `What is bought`, `Why safer with us` with 4 numbered blocks, `CTA`.
Do not include citations, source markers, or notebook residue inside the text.

## Required logic order

1. mandatory launch context
2. what is being bought
3. `Dominanta` value
4. `genu.mark` core value
5. `Logictime` implementation discipline
6. next step CTA

## Self-check

Rewrite if the draft:
- sounds like company bragging rather than safer implementation guidance;
- does not make `genu.mark` the thing being bought;
- blurs `Dominanta` and `Logictime` roles;
- introduces unsourced entities or platform layers;
- uses weak superiority claims;
- leaves the buyer unable to retell why this is safer.
