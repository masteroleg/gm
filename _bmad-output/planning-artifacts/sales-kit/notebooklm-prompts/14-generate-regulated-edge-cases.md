# Generate Edge Cases / Regulated

Use only the uploaded sources.

## Hard rules

- Keep `genu.mark` as the line core.
- Restate the external contour split before discussing exceptions.
- Treat ERP handoffs, offline behavior, and rework as real implementation risk.
- Print these exact lines in the boundary restatement:
  - `ERP -> source of production job`
  - `ERP / internal systems <- exchange of results and statuses`
- Make the maturity difference between weak and strong implementation visible.
- Do not introduce any new product, architecture layer, or platform explanation not present in the uploaded sources.
- Do not use weak superiority claims.
- Do not use unsafe claims such as `гарантує`, `найкраще`, `під ключ`, `єдина відповідальність`, or `повний пакет`.
- Keep the output in modern, buyer-facing Ukrainian.
- End with fit review / technical session / pilot.

## Output contract

Return each section as `Section title`, `Risk explained`, `What must be controlled`, `Buyer conclusion`.
Do not include citations, source markers, or notebook residue inside the text.

## Self-check

Rewrite if the draft:
- reads like a generic technical checklist instead of proof of maturity;
- does not restate the approved boundary model;
- skips handoff risk, offline, or rework;
- introduces unsourced entities;
- uses phrases like `гарантує`, `compliance`, `відповідність` as a promise, or other overclaims without proof;
- leaves the buyer unable to retell why this team understands real production better.
