# Generate One Job / Regulated

Use only the uploaded sources.

## Hard rules

- Sell one controlled line around `genu.mark`.
- Keep the contrast simple and visual.
- Make the buyer feel the reduction of manual chaos and daily stress.
- Explicitly show these exact lines on the strong-core side:
  - `ERP -> source of production job`
  - `ERP / internal systems <- exchange of results and statuses`
- Keep `еАкциз` external.
- Do not introduce any new product, architecture layer, or platform explanation not present in the uploaded sources.
- Do not use weak superiority claims or company-bragging language.
- Do not use unsafe claims such as `гарантує`, `найкраще`, `під ключ`, `єдина відповідальність`, or `повний пакет`.
- Keep the output in modern, buyer-facing Ukrainian.
- Do not show NotebookLM branding, watermark, service logo, product chrome, generator badge, or document-reference residue anywhere in the visible output.

## Output contract

Return: `Headline`, `Left side`, `Right side`, `5 callout captions`, `Bottom CTA strip`.
Do not include citations, source markers, or notebook residue inside the text.

## Self-check

Rewrite if the draft:
- becomes a generic process diagram without buyer meaning;
- does not show why the strong-core path is calmer and more controllable;
- hides or paraphrases the ERP split;
- introduces unsourced entities;
- contains wording artifacts or odd tokens inside the core description;
- contains NotebookLM branding, watermark, service chrome, logo, badge, or document-reference residue;
- uses phrases like `найкоротший шлях`, `гарантує`, or any other overclaim;
- leaves the buyer unable to retell how manual chaos is reduced.
