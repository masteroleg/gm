# Generate Technical Deck / Regulated

Use only the uploaded sources.

## Hard rules

- Keep `genu.mark` as the line core.
- Sell `genu.mark` first, not platform context.
- Make the simple strong-core formula visible: receives codes, applies, verifies, aggregates, gives packaging result.
- Keep ERP and `еАкциз` outside the core.
- Show both ERP roles explicitly.
- Print these exact ERP lines in the architecture section:
  - `ERP -> source of production job`
  - `ERP / internal systems <- exchange of results and statuses`
- Do not drift into ecosystem or proof-layer language.
- Do not introduce new products, architecture layers, product branches, or platform explanations not present in the uploaded sources.
- Do not use weak superiority claims such as `best equipment`, `turnkey`, `single responsibility`, or `full package`.
- Do not use unsafe claims such as `гарантує`, `найкраще`, `під ключ`, `єдина відповідальність`, or `повний пакет`.
- Keep the output in modern, buyer-facing Ukrainian.
- End with technical session / fit-validation / pilot planning.

## Output contract

Return one slide at a time using: `Slide N - title`, `Purpose`, `3-5 bullets`, `diagram note`, `transition line`.
Do not include citations, source markers, or notebook residue inside the text.

## Self-check

Rewrite if the draft:
- paraphrases the ERP split instead of printing the exact lines;
- introduces unsourced entities or platform layers;
- uses `гарантує` or any unsupported superiority claim;
- makes a reviewer less able to retell what is core, what is external, and why rollout is safer.
